# ─── Advisory Evaluation Environment ────────────────────────
# ETAP 8.5 Snapshot — Isolated from production

# OpenAI (dedicated key for evaluation — do NOT reuse production key)
OPENAI_API_KEY=sk-...

# Neon / Postgres (separate DB from production)
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/advisory_evaluation?sslmode=require

# Admin access token (protect /admin route)
ADMIN_TOKEN=change-me-before-deploy

# App URL (for CORS and links)
NEXT_PUBLIC_APP_URL=http://localhost:3010
